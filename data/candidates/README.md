# Candidates

This directory is outside the canonical loader and release path. Candidate
import tooling is not part of the MVP. Nothing here becomes canonical without
manual curator promotion into a PHNER template and full validation.

